# /home/zhangyc/mysql/docker-compose.yml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: my-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ZYCzyc520@APEX!   # 🔑 必须修改！
      # 可选：创建默认数据库和用户
      # MYSQL_DATABASE: myapp_db
      # MYSQL_USER: appuser
      # MYSQL_PASSWORD: AppPass456!
    ports:
      - "3306:3306"          # 宿主机:容器
    volumes:
      - ./data:/var/lib/mysql                # 数据持久化
      #- ./conf/my.cnf:/etc/mysql/conf.d/my.cnf:ro  # 自定义配置（可选）
      - ./logs:/var/log/mysql                # 日志（可选）
    command: >
      --default-authentication-plugin=mysql_native_password
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "bash", "-c", "timeout 5 bash -c '</dev/tcp/127.0.0.1/3306'"]
      timeout: 20s
      retries: 10
      start_period: 30s
